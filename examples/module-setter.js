// example - valid: true
angular.module('myModule', [])

// example - valid: false, errorMessage: "Declare modules without a variable using the setter syntax."
var app = angular.module('myModule', []);
